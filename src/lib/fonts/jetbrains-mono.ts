import { JetBrains_Mono } from "next/font/google";

/** An instance of the Jetbrains Mono font. */
export const jetbrainsMono = JetBrains_Mono({
    variable: "--font-jetbrains-mono",
    display: "swap",
    subsets: ["latin"],
});
